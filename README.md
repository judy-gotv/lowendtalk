

第二步:创建 KV 命名空间

命名空间：KV_CONFIG



第二步:Worker.js 代码在单独创建一个  把js里面的代码放进去
下面是变量

| 变量名          | 内容                     |
| ------------ | ---------------------- |
| `BOT_TOKEN`  | Telegram Bot Token     |
| `CHANNEL_ID` | 你的频道 ID（-100xxxxxxxxx） |
| `KV_CONFIG`  | 已绑定（保存已推送列表等）          |


第三步:
zip文件单独创建一个Worker（注释: 先下载到本地，解压出来把里面的api地址改成第二步的（你部署好的）域名）
